// primitive data types

const a =10; //init
const boolean = true;
const srting = 'char';
const string1 = "Statement 2";
const string2 = `Statement ${value}`;

// Non primitive data types

const arr =[1,2,'ch', {object : Value}, ['a', 'b', 'c']];

const object = {
    key: value
}

// name function

function add (a, b) {
     return a+b;
    }

const sum = function (a,b) {
    return a + b;
}

// arrow function

const sum1 = (a, b) => {
    return a +b;
}

